def FindMax(inputArray):
    max = inputArray[0] 
    for i in range:
        i < len(inputArray) 
        if inputArray[i] > max:
            max = inputArray[i]
            i = i + 1
    return max 